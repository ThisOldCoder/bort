= title "Forgot Your Password?", :h2
- form_for @password do |f|
  = f.error_messages
  - field_set_tag 'Enter Your Email Address' do
    = f.text_field :email, :help => 'a password reset email will be sent' + |
                                    ' to this address' |
  .buttons
    = f.submit "Submit"
